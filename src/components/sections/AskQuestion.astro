<div class="cont cont--spacing ask-question">
<div class="inner">

<div class="c-1 ask-question__block">
</div>

<div class="c-2 c-4-tablet">
  <form class="ask-question__form">
  <h1 class="ask-question__heading">Har du en fråga?</h1>
  <p>Fyll i formuläret här bredvid om du har frågor kring bokning, tillgänglighet, priser eller något annat relaterat till stugan Brusckvallen i Bruksvallarna. Vi återkommer till dig så snart som möjligt med svar!</p>
  <label for="name">Namn:</label>
  <input name="name" placeholder="Johan Andersson" />
  <label for="email">E-post:</label>
  <input name="email"  placeholder="johan.andersson@gmail.com" />
  <label for="text">Medelande:</label>
  <textarea name='text'></textarea>
  <button>Skicka medelande</button>
</form>

</div>

</div>
</div>

<style lang="scss">
@import '../../styles/variables.scss';

.ask-question__block {
  @include break(tablet, down) {
    display: none;
  }
}

.ask-question {
  padding-top: spacing(7);
  padding-bottom: spacing(8);

  background-color: var(--background-secondary);
  background-color: var(--primary-light);

  @include break("phone", down) {
    padding-top: spacing(4);
    padding-bottom: spacing(4);
  }
}

.ask-question__heading {
  margin-bottom: spacing(1);
}

.ask-question__form {
  @include break("phone", down) {
    margin-top: spacing(2);
  }

  label {
    margin-bottom: spacing(0.5);
  }
  
  p {
    margin-bottom: spacing(2);
  }

  input {
    margin-bottom: spacing(2);
  }

  button {
    padding: spacing(1.5, 3);
    margin-top: spacing(3);
    cursor: pointer;
    border: none;
    background-color: #333;
  background-color: var(--primary);
    font-size: 1rem;
    font-family: var(--font-family-heading);
    font-weight: 700;
    color: white;
    width: 100%;
  }

  * {
    display: block;
  }
}
</style>
